div
	a.thumbnail(href="{{ '/detail/flow/' + flow.id }}")
		span(class="device Flow")
			i.fa.fa-fw.fa-random
		code.dp
			| {{ flow.nw.src }}:{{ flow.tp.src }} -> {{ flow.nw.dst }}:{{ flow.tp.dst }}
			| [{{ flow.nw.protocol | networkProtocol }}]
	| <br />