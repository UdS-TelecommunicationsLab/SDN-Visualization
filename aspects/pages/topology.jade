div#sdn-topology
    div.row
        div.col-sm-8.col-md-9
            div.panel.panel-default
                div.panel-heading
                    div.topologyStats.pull-right
                        span.label.label-primary
                            i.fa.fa-hdd-o
                            | &nbsp;
                            span Switches: {{ data.nvm.devices | countSwitches }}
                        | &nbsp;
                        span.label.label-primary
                            i.fa.fa-mobile-phone
                            | &nbsp;
                            span Clients: {{ data.nvm.devices | countClients }}
                        | &nbsp;
                        span.label.label-primary()
                            i.fa.fa-random
                            | &nbsp;
                            span Flows: {{ data.nvm.flows.length }}
                    i.fa.fa-globe
                    | &nbsp;Topology
                div.panel-body
                    sdn-topology-map(height="{{ height }}", styles="styles", visibility-button="true", show-inactive="true")
        div.col-sm-4.col-md-3
            div.panel.panel-default
                div.panel-heading
                    i.fa.fa-random
                    | &nbsp;Flows
                div.table-responsive
                    table.table.table-condensed.table-striped
                        thead
                        tbody
                            tr(ng-repeat="flow in data.nvm.flows", ng-mouseenter="highlightFlow(flow)",ng-mouseleave="blurFlow(flow)")
                                td(width="24px")
                                    a(href="{{ '/detail/flow/' + flow.id }}")
                                        span(class="device Flow", ng-switch="flow.nw.protocol")
                                            strong(ng-switch-default="") {{ flow.nw.protocol | networkProtocol }}
                                td
                                    code.dp S: {{ flow.nw.src }}:{{ flow.tp.src }}
                                td
                                    code.dp D: {{ flow.nw.dst }}:{{ flow.tp.dst }}
                            tr(ng-show="data.nvm.flows.length == 0")
                                td(colspan=2) No flows at the moment.